{
  "name": "music-discovery-app",
  "version": "1.0.0",
  "description": "A music discovery app using iTunes API and json-server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "dev": "json-server --watch db.json --port 3000 & python3 -m http.server 8080"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}